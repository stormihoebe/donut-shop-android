<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.epicodus.myrestaurants.MainActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:srcCompat="@drawable/donuts"
        android:scaleType="centerCrop"
        android:id="@+id/donutsBackground" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lotsa Donuts"
        android:textSize="40sp"
        android:textStyle="normal|bold"
        android:textColor="@color/colorTextIcons"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="36dp"
        android:textAlignment="center"
        android:id="@+id/myRestaurantsTextView" />

    <Button
        android:text="Find Donuts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/emberButton"
        android:textSize="18sp"
        android:textColor="@color/colorTextIcons"
        android:background="@color/colorAccent"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="phone"
        android:ems="10"
        android:id="@+id/enterZip"
        android:layout_above="@+id/emberButton"
        android:layout_centerHorizontal="true"
        android:background="@color/colorTextIcons"
        android:hint="Enter Zip Code"
        android:textAlignment="center"
        android:textSize="20sp" />

</RelativeLayout>
